# Correspondencia entre el original Python y este port

Origen: `jwadow/kiro-gateway`, v2.4.dev.13, commit `a5292ca`.

Regla: el nombre del paquete Go es el del módulo Python sin guiones bajos. La columna
`Ficheros Go` se rellena a medida que cada fase implementa su paquete.

| Módulo Python | Paquete Go | Ficheros Go |
|---|---|---|
| `main.py` | `cmd/kiro-gateway` + `internal/server` | `cmd/kiro-gateway/main.go`, `internal/server/server.go`, `internal/server/middleware.go`, `internal/server/server_test.go` (Task 11, la última de la fase 5. `internal/server`: `Server`/`New` monta `routesopenai.New`+`routesanthropic.New` (accounts/client/cfg ya inicializados por el llamador) sobre un `http.ServeMux` con las 6 rutas (`GET /{$}`, `GET /health`, `GET /v1/models`, `POST /v1/chat/completions`, `POST /v1/messages`, `POST /v1/messages/count_tokens` — el patrón `/{$}` del ServeMux mejorado de Go 1.22+ es necesario para que "/" NO capture como subárbol las rutas sin match, que deben devolver 404) envuelto en middleware CORS→auth→panic-recovery (`middleware.go`, spec §5.4, en ese orden de fuera a dentro). `GET /` y `GET /health` (§7.1) mantienen `status`/`message`/`version` y `status`/`timestamp`/`version` de `routes_openai.py:93-120` y añaden `active_account` (vía `accounts.GetFirstAccount().ID`, `null` si no hay cuenta), `uptime_seconds` (`time.Since` desde `Server.Start`) y `mode` (literal fijo `"ACCOUNT_SYSTEM"`: el port no implementa el modo legacy sin accountmanager que `main.py:383-451` sí soporta) — extensión del spec (`docs/superpowers/specs/2026-09-04-kiro-gateway-go-port-design.md:742-743`, "añade cuenta activa, uptime y modo"), sin equivalente en el upstream real. **Auth por endpoint** (`authMiddleware`): `/v1/models`+`/v1/chat/completions` exigen `Authorization: Bearer <PROXY_API_KEY>` (`verify_api_key`, `routes_openai.py:68-86`); `/v1/messages`+`/v1/messages/count_tokens` aceptan `x-api-key` O `Authorization: Bearer` (`verify_anthropic_api_key`, `routes_anthropic.py:73-112`); `/` y `/health` sin auth. `anthropic-version` se acepta y NUNCA se valida (spec §7.1) — ausencia de código deliberada, no un olvido. **Panic recovery** (`recoverMiddleware`) captura los 3 `panic()` deliberados de fase 3 (`converterscore/payload.go:238,259` + `convertersopenai/converters.go:522`, equivalentes a `ValueError` sin hueco en la firma Go) y cualquier otro panic no anticipado, devolviendo 500 en el dialecto de error del path (`classify`, por prefijo de ruta): OpenAI `{"error":{"message","type":"kiro_api_error","code"}}` o Anthropic `{"type":"error","error":{"type","message"}}`. Estos dos sobres (`writeOpenAIError`/`writeAnthropicError`) están DUPLICADOS a propósito de `routesopenai`/`routesanthropic` (sus versiones no están exportadas — instrucción explícita del brief de no exportarlas). **CORS** (`corsMiddleware`): `Access-Control-Allow-Origin/Methods/Headers: "*"` + `Access-Control-Allow-Credentials: true` en TODA respuesta, y cualquier `OPTIONS` corta con 200 antes de auth/mux — simplificación deliberada frente a `starlette.middleware.cors.CORSMiddleware` (vendorizado en `.upstream/.venv/.../starlette/middleware/cors.py`, montado en `main.py:548-554` con `allow_origins=["*"], allow_credentials=True, allow_methods=["*"], allow_headers=["*"]`): el original refleja el `Origin` real en preflights con credentials (`cors.py:38-46,100-101`) en vez de un `"*"` literal, expande `allow_methods=["*"]` a la lista de 7 verbos (`cors.py:18,29-30`) y solo activa CORS si la petición trae cabecera `Origin` (`cors.py:85-87`) — el brief de esta tarea (§5.4, punto 1) pide expresamente el comportamiento simple, que es lo que implementa este middleware. **Seam de test**: 4 campos no exportados de `Server` (`openaiModels`, `openaiChatCompletions`, `anthropicMessages`, `anthropicCountTokens`) resueltos por el mux en cada petición (no capturados por valor), mismo patrón que `Handler.apiURL` de `routesopenai`/`routesanthropic` — permite que `server_test.go` sustituya un stub 200 fijo en las rutas que golpearían Kiro real (`/v1/messages`, `/v1/chat/completions`) sin tocar red, ya que `apiURL` de esos handlers no es alcanzable desde `internal/server`. `Server.Start(ctx)` fija `startedAt` y bloquea en `ListenAndServe` hasta que `ctx` se cancela o el listener falla por sí solo; NO cierra el servidor por su cuenta — `Shutdown(ctx)` es responsabilidad de `main.go`, invocado desde una goroutine que observa `ctx.Done()` en paralelo, con su propio timeout de 30s (independiente de `ctx`, ya cancelado en ese punto). **`cmd/kiro-gateway/main.go`**: CLI `-H/--host`, `-p/--port`, `-v/--version`, `-h/--help`, `--health` (spec §7.3, precedencia flag>env>default vía `config.Options{Host,Port}`); `--health` es una ADICIÓN sin precedente en el `argparse` original (GET a `/health` del servidor en marcha, imprime el cuerpo, sale 0 si 200 o 1 si no — apunta a `127.0.0.1` cuando el host resuelto es `0.0.0.0`/vacío, porque esa es una dirección de bind, no de destino). `--version` imprime SOLO `version.Version()` (p.ej. `"2.4.dev.13+go"`), sin el prefijo `"Kiro Gateway "` del stub de fase 5 ni el `"%(prog)s "` de `argparse` (`main.py:641-644`: `python main.py -v` imprime `"main.py 2.4.dev.13"`) — el criterio de cierre de esta tarea exige literalmente que `--version` imprima `"2.4.dev.13+go"`, así que se imprime solo esa cadena. El brief de esta tarea cita `internal/version.String()`; esa función no existe (verificado por grep) — se usa `version.Version()`, la real. Ciclo de vida de `runServer`: `config.Load` → wire de las 7 package vars de `converterscore` (`FakeReasoningEnabled/MaxTokens/BudgetCap`, `TruncationRecoveryEnabled`, `ToolDescriptionMaxLength`, `AutoTrimPayload`, `KiroMaxPayloadBytes` — config de módulo mutable de fase 3, cablearla aquí es el trabajo explícito de esta tarea) → `httpclient.New` → `accountmanager.NewManager` → `LoadCredentials(ctx)` → `Initialize(ctx)` → `SaveStatePeriodically(ctx)` en goroutine → `server.New` → `Start(ctx)`, con `signal.NotifyContext(SIGINT, SIGTERM)` para el cierre ordenado.) |
| `config.py` | `internal/config` | `config.go`, `parse.go`, `dotenv.go` |
| `utils.py` | `internal/utils` | `fingerprint.go`, `ids.go`, `headers.go` |
| `tokenizer.py` | `internal/tokenizer` | `bpe.go`, `pretoken.go`, `vocab.go`, `cl100k_base.tiktoken`, `requesttokens.go` (Task 8 añade `requesttokens.go`: `CountTokens`/`CountMessageTokens`/`CountToolsTokens` — relocalizados desde `internal/streamingopenai/tokencount.go` (Task 7), que los tenía como funciones privadas del paquete — más `CountSystemTokens`, nuevo, para el tercer parámetro de `estimate_request_tokens` (`system_prompt`, `tokenizer.py:256-293`) que `streaming_anthropic.py` necesita y `streaming_openai.py` no usa. Ver ledger en `task-8-report.md`: ambos paquetes de formatter (`streamingopenai`, `streaminganthropic`) consumen estas funciones vía `internal/tokenizer`; sus dos call sites de streaming (`streaming_openai.py:317-320` fallback, `streaming_anthropic.py:177-183` message_start vía `estimate_request_tokens`) pasan `apply_claude_correction=False`. El parámetro `applyCorrection bool` SÍ se porta en `CountMessageTokens`/`CountToolsTokens`/`CountSystemTokens` (Task 8 fix round 1) porque el endpoint `/v1/messages/count_tokens` (`routes_anthropic.py:948-952`, Task 10) llama `estimate_request_tokens(apply_claude_correction=True)`. Igual que upstream, `applyCorrection` escala SOLO el total final por `CLAUDE_CORRECTION_FACTOR` (1.15) truncando hacia cero (`int()`), nunca los sub-conteos por-string (que siempre pasan `false`). No se porta `estimate_request_tokens` como función propia: sus tres llamadas (`count_message_tokens` + `count_tools_tokens` + `count_system_tokens`) se inlinean directamente en `streaminganthropic.Formatter.EmitMessageStart`, ver task-8 ledger.) |
| `parsers.py` | `internal/parsers` | `parser.go`, `toolcalls.go`, `brace_scanner.go`, `dedup.go`, `truncation.go` (Task 1 porta find_matching_brace, deduplicate_tool_calls, parse_bracket_tool_calls y el state machine AwsEventStreamParser.feed/get_tool_calls/reset — ports de `parsers.py:39-569`. `Event.Kind` usa los 3 tipos que `_process_event` realmente emite (`content`/`usage`/`context_usage`, .upstream/kiro/parsers.py:308-332), no los 7 nombres de prefijo: `tool_start`/`tool_input`/`tool_stop` nunca producen evento en el original — solo mutan estado — y `followupPrompt` como prefijo más temprano no tiene rama en absoluto (cae al `return None` final). `Finish()` y el Kind `tool_call` son adiciones Go-only sin equivalente upstream (el original expone tool calls solo vía `get_tool_calls()`, que también se porta 1:1 como `Parser.GetToolCalls()`). Los ids de `parse_bracket_tool_calls` dependen de un contador de sesión no reproducible fuera de la suite completa de grabación (docs/CORPUS.md §"contadores globales de sesión"); el test de corpus compara forma (`call_<8 hex>`), no valor, igual que ya hace el port para chatcmpl-/msg_-hex. Test diferencial `bytes.ToValidUTF8` vs `decode('utf-8', errors='ignore')` (spec §6.4/§8.4, pendiente desde fase 2) añadido en `differential_test.go`, 1000 semillas fijas, verificado contra python3 real.) |
| `thinking_parser.py` | `internal/thinkingparser` | `state_machine.go`, `parser_test.go` (Task 2 porta el FSM de tres estados (PreContent/InThinking/Streaming) que detecta 4 tags de razonamiento (`<thinking>`, `<think>`, `<reasoning>`, `<thought>`) al inicio del stream. Implementa buffering prudente con `max_tag_length = max(len(tag)) * 2` para evitar partir tags entre chunks. Feed/Finish devuelven `(thinking, content)` en lugar del `ThinkingParseResult` completo del original, y aplican el handling mode internamente en lugar de dejar `process_for_output` como método separado.) |
| `truncation_state.py` | `internal/truncationstate` | `cache.go`, `cache_test.go` (Task 3 porta los dos `dict` protegidos con `threading.Lock` como caches de `any` con protección `sync.Mutex`, índice de contenido con SHA256 hash del primer 500 caracteres, Get destructivo — `map.pop(key)` ↔ `delete(map, key)` — retorna `false` si no existe. Tests hand-written: SetTool + GetTool retorna y elimina, SetContent + GetContent con SHA256, concurrencia con dos fases secuenciales: 10 goroutines para set, 10 para get.) |
| `truncation_recovery.py` | `internal/truncationrecovery` | `recovery.go`, `recovery_test.go` (Task 4 porta `generate_truncation_tool_result(tool_name, tool_use_id, truncation_info) -> dict` y `generate_truncation_user_message() -> string` y `should_inject_recovery() -> bool` del original. Implementa un package var `truncationRecoveryEnabled` que espeja la bandera de converterscore; los tests pueden sobrescribir localmente. 17 fixtures de corpus (13 para tool_result, 2 para user_message, 2 para should_inject, con algunos casos usando kwargs en lugar de args, y TRUNCATION_RECOVERY tanto true como false en config). Tests manejan ambas formas de input (args vs kwargs) y leen/aplican la config dinámicamente.) |
| `payload_guards.py` | `internal/converterscore` | `payloadtrim.go`, `payloadtrim_test.go` (Task 9 fase 6a. Ports de `payload_guards.py:46-164` — las 5 funciones `check_payload_size` (medición con `json.Encoder`/`SetEscapeHTML(false)` para matching Python byte-count), `_strip_empty_tool_uses`, `_align_to_user_message`, `_repair_orphaned_tool_results` (preserva texto huérfano con marcador), y `trim_payload_to_limit` (recorta pares desde frente, min 2 entradas). NO se porta `PayloadTrimStats` (YAGNI: sin logging ni stats necesarias en esta tarea). Integrado en `payload.go` bajo el stub `if AutoTrimPayload { TrimPayloadToLimit(...) }` (línea 375-376), gateado por el flag. Consumido SOLO por `BuildKiroPayload` de esta tarea; el corpus de 83 casos fija `AUTO_TRIM_PAYLOAD=false` (nunca lo ejercita), así que paridad en output sin nuevos defectos.) |
| `cache.py` | `internal/cache` | `cache.go`, `cache_test.go` (Task 1 de fase 6a porta `ModelInfoCache` completa — no solo `update`/`get_max_input_tokens` del brief mínimo, sino toda la superficie que Task 2 (`modelresolver`) y los formatters de streaming necesitan: `Update` (cache.py:65, reemplaza el contenido completo indexado por `modelId`), `Get` (cache.py:80), `IsValidModel` (cache.py:92), `AddHiddenModel` (cache.py:106, misma forma de dict: `modelId`/`modelName`/`description`/`tokenLimits.maxInputTokens`/`_internal_id`/`_is_hidden`), `GetMaxInputTokens` (cache.py:129, con el chequeo "truthy" de Python vía `asTruthyInt` — `0`, ausente o nil caen al default igual que `.get(...) or DEFAULT`), `IsEmpty` (cache.py:144) e `IsStale` (cache.py:153, `lastUpdate` cero o `now-lastUpdate > ttl`). `asyncio.Lock` → `sync.RWMutex` (RLock en getters, Lock en `Update`/`AddHiddenModel`). `DEFAULT_MAX_INPUT_TOKENS` (200000, config.py:300) es la constante exportada `DefaultMaxInputTokens`; `MODEL_CACHE_TTL` (3600, config.py:297) no se hardcodea — `New(ttlSeconds int)` lo recibe del llamador, sin `os.Getenv` en este paquete hoja. Los valores del cache siguen siendo `map[string]any` sin tipar, igual que los dicts de modelo del original. Tests hand-written (sin corpus para `cache.py`): default para modelo desconocido, `Update` con límite explícito, límite "falsy" (0) cae al default, `IsValidModel`/`Get`/`IsEmpty` antes y después de `Update`, `AddHiddenModel` (crea con default y es no-op si ya existe), `IsStale` en sus tres estados, y concurrencia de dos fases (N goroutines `Update` completas, luego N goroutines de lectura) — mismo patrón que `internal/truncationstate/cache_test.go`, ya que el proyecto corre sin `-race`.) |
| `model_resolver.py` | `internal/modelresolver` | `normalize.go`, `catalog.go`, `resolver.go`, `resolver_test.go` (Task 2 fase 6a. `normalize.go`: `NormalizeModelName` (guiones→puntos, quita fechas `YYYYMMDD`, quita `-latest`, formato invertido `claude-4.5-opus-high`→`claude-opus-4.5`, sufijos de ventana `[1m]`/`[200k]`, formato antiguo `claude-3-7-sonnet`→`claude-3.7-sonnet`), `ToRuntimeModelID`, `ExtractModelFamily` — ports de `model_resolver.py:51,87-190,222`. `catalog.go`: `FallbackModels` (13 modelos del catálogo estático, §6.12), `Aliases` (`auto-kiro`→`auto`), `HiddenFromList` (`auto`) — origen de verdad en `kiro.config.FALLBACK_MODELS`. `resolver.go`: `GetModelIDForKiro(modelName, hiddenModels)` (normaliza + lookup de modelos ocultos, `model_resolver.py:192-220`), `ModelResolution{RuntimeModelID,Normalized,Source,IsVerified}`, `ModelResolver.Resolve` (4 capas: 0 alias, 1 cache hit crudo, 2 normaliza+cache, 3 ocultos, else passthrough — `model_resolver.py:301-360`), `GetAvailableModels` (unión cache∪ocultos∪aliases menos `HiddenFromList`, para `/v1/models` — `model_resolver.py:370-397`). Consume `internal/cache` (Task 1): `IsValidModel`, `GetAllModelIDs`, `AddHiddenModel`. Task 3 sustituye el port parcial `getModelIDForKiro`+`HiddenModels` de `convertersopenai`/`convertersanthropic` por este paquete. **Recovery**: el implementer stalleó 3× (watchdog de infra) tras escribir el código pero antes del commit; el coordinador verificó verde (tests del paquete + `go test ./...` 31 pkgs) y commiteó su trabajo. La review de tarea verifica independientemente la fidelidad de `NormalizeModelName` contra el Python real.) |
| `kiro_errors.py` | `internal/kiroerrors` | `enhance.go` |
| `network_errors.py` | `internal/networkerrors` | `types.go`, `classify.go`, `messages.go` |
| `account_errors.py` | `internal/accounterrors` | `classify.go` |
| `exceptions.py` | `internal/validationerrors` | `validation.go` |
| `models_openai.py` | `internal/modelsopenai` | `models.go` |
| `models_anthropic.py` | `internal/modelsanthropic` | `blocks.go`, `models.go` |
| `converters_core.py` | `internal/converterscore` | `types.go`, `extract.go`, `text.go`, `tools.go`, `images.go`, `normalize.go`, `thinking.go`, `payload.go` |
| `converters_openai.py` | `internal/convertersopenai` | `converters.go` |
| `converters_anthropic.py` | `internal/convertersanthropic` | `converters.go` |
| `streaming_core.py` | `internal/streamingcore` | `events.go`, `pipeline.go`, `pipeline_test.go` (Task 6 porta la clase KiroEvent y el pipeline que une parsers.Parser (Task 1) y thinkingparser.Parser (Task 2) en un stream de eventos unificado. El pipeline procesa eventos del parser y aplica thinking splitting: para cada evento "content", extrae el valor, lo pasa al thinking parser, y emite eventos "thinking" (si hay) y "content" (si hay) en ese orden. Maneja también "usage" → UsageData, "context_usage" → float, "tool_call" → ToolUseData. Si hay diagnóstico de truncamiento, emite un evento "error" con el mensaje del diagnóstico. Feed() procesa un chunk, Finish() vacía buffers pendientes.) |
| `streaming_openai.py` | `internal/streamingopenai` | `formatter.go`, `tokencount.go`, `formatter_test.go`, `bracket_test.go` (Task 7, fix round 2. Formatter struct mantiene estado: completionID (chatcmpl-<32hex>), model, created (timestamp unix), firstChunk, acumuladores de content/thinking, tool calls, `creditsUsedRaw` (bytes JSON crudos del evento "usage", ecoados tal cual en `usage.credits_used` — ver `streaming_openai.py:405-406`, `final_chunk["usage"]["credits_used"] = metering_data`, que no es siempre un float), `contextUsagePercentage`, `requestMessages`/`requestTools` (JSON crudo, para el fallback de `count_message_tokens`/`count_tools_tokens` cuando Kiro no manda `context_usage_percentage` — streaming_openai.py:317-323; el corpus muestra que esto ocurre en 39/40 fixtures, no es un caso raro). `tokencount.go` (Task 8 update: ahora solo `isJSONTruthy`, dialect-specific — `count_tokens`/`count_message_tokens`/`count_tools_tokens` se movieron a `internal/tokenizer/requesttokens.go` como funciones exportadas, reutilizadas por `streaminganthropic`; `formatter.go` llama `tokenizer.CountTokens`/`CountMessageTokens`/`CountToolsTokens` en vez de las versiones locales que tenía hasta el fix round 2 — ver ledger en `task-8-report.md`) reimplementaba `count_tokens`/`count_message_tokens`/`count_tools_tokens` de `kiro/tokenizer.py` con paridad exacta, incluyendo el factor de corrección Claude (1.15, solo aplicado a completion_tokens) y `pyjson.Dumps` para los sub-JSON (tool `parameters`/`input_schema`, tool_use `input`) cuyo orden de claves afecta el conteo de tokens. Finish() calcula completion_tokens vía tokenizer+corrección, determina finish_reason (truncation vs tool_calls vs stop, replicando el orden de streaming_openai.py:296-302). **Fix de la review whole-branch de fase 5**: `Finish` detecta tool calls estilo `[Called fn with args: {...}]` en `fullContent` vía `parsers.ParseBracketToolCalls`, los fusiona con los del stream y deduplica con `parsers.DeduplicateToolCalls` ANTES de emitir el chunk tool_calls y de calcular finish_reason/content_was_truncated (streaming_openai.py:276-279). El paso de dedup existe en el upstream OpenAI pero NO en el Anthropic, así que `streaminganthropic.Finish` correctamente no lo aplica — divergencia legítima entre dialectos. Cubierto por `bracket_test.go` (ningún fixture del corpus ejercita `[Called`, grep=0). Este cableado se señaló en task-7-report para Task 9 y se cayó en el traspaso; lo cierra la review whole-branch. Finish() también calcula prompt/total_tokens vía `calculate_tokens_from_context_usage` (streaming_core.py:337-362, incluyendo la división `/100` que el fix round 1 omitía) con fallback a `count_message_tokens`+`count_tools_tokens`. 39/40 fixtures del corpus pasan; el fixture restante (`c272a07c05e0e8a5`) depende de un mock de `parse_bracket_tool_calls` en el test Python de origen que no es reproducible desde los datos del fixture — ver `task-7-report.md`. Task 8 convierte ese fallo en un `t.Skip` explícito con cita de línea, en vez de dejarlo como fallo silencioso sin marcar (mismo caso que 4 fixtures de `streaminganthropic`, ver abajo). Configurable thinking handling mode: AsReasoningContent vs AsContent.) |
| `streaming_anthropic.py` | `internal/streaminganthropic` | `formatter.go`, `block_index.go`, `finish.go`, `payloads.go`, `formatter_test.go` (Task 8. `block_index.go`: `blockIndex` — índice de bloque corriente + estado open/index para thinking y text; tool_use se abre y cierra átomicamente dentro de una sola llamada (`ReserveToolBlock`), sin estado persistente. `Formatter`: `messageID` (`msg_<24hex>`, `utils.GenerateMessageID`), `model`, `thinkingHandling` (as_reasoning_content/include_as_text/strip), `thinkingSignature` (`sig_<32hex>`, generado una vez en `New`, reusado en cada bloque thinking — igual que el original, que genera un único `thinking_signature` antes del loop), acumuladores fullContent/fullThinkingContent, toolBlockCount, receivedContextUsage, cache_read/creation_input_tokens (punteros, omitempty — solo aparecen en el `usage` de `message_delta` si un evento "usage" los trajo), requestMessages/requestTools/requestSystem (JSON crudo). `EmitMessageStart` calcula `input_tokens` inline con `tokenizer.CountMessageTokens`+`CountToolsTokens`+`CountSystemTokens` (streaming_anthropic.py:176-183, `estimate_request_tokens(apply_claude_correction=False)` — no se portó como función separada, sus tres llamadas se inlinearon). `Handle` despacha por `Kind`: "content" cierra thinking si está abierto y abre/reusa el bloque text; "thinking" se ramifica por `thinkingHandling` (bloque thinking nativo / fold a texto / strip silencioso, pero `fullThinkingContent` acumula SIEMPRE, incluso en strip — afecta output_tokens); "tool_use" cierra thinking y text, emite el triplete start/delta/stop; "context_usage" solo marca `receivedContextUsage=true` (el pipeline ya filtra `is not None`); "usage" fusiona campos de caché vía `_extract_cache_usage_fields` (streaming_anthropic.py:101-126). `Finish`: detecta tool calls estilo `[Called f with args: {...}]` post-loop vía `internal/parsers.ParseBracketToolCalls` (ya portado en Task 1 — NO se reimplementó), cierra bloques que sigan abiertos, calcula `content_was_truncated` (sin context_usage + hay texto + cero tool blocks), `output_tokens` vía `tokenizer.CountTokens(fullContent+fullThinkingContent, true)` (con corrección Claude — este call site SÍ usa el default `apply_claude_correction=True`, a diferencia de `estimate_request_tokens`), `stop_reason` (max_tokens > tool_use > end_turn), y emite `message_delta`+`message_stop`. **Efecto secundario DIFERIDO — persistencia de truncation-state**: upstream `stream_kiro_to_anthropic`, tras `message_stop` (streaming_anthropic.py:665-686), si `should_inject_recovery()` (Task 4, devuelve `TRUNCATION_RECOVERY`, **default true** — config.py:333) persiste la info de truncación en los caches de `internal/truncationstate` (Task 3) vía `save_tool_truncation`/`save_content_truncation`, recolectando además `truncated_tools` durante el loop de tool_use (chequeo `_truncation_detected`, streaming_anthropic.py:470-475) — para que la SIGUIENTE petición inyecte los mensajes de recovery. Este port NO ejecuta ese save en `streaminganthropic.Finish`: no tiene salida observable en SSE (por eso ningún fixture del corpus lo ejercita y el port pasa igual) y requiere los paquetes `truncationstate`+`truncationrecovery`, que el formatter no recibe. **Pendiente para Task 10/routesanthropic** (que orquesta request/response y ya leerá truncation-state para inyectar recovery): tras cerrar el stream debe recolectar los tool calls truncados (vía el Kind `"error"`/diagnosis que `Handle` hoy ignora a propósito, formatter.go:125-129, o `parsers.Parser.TruncationDiagnosis`) y el `full_content`, y llamar el save gated por `truncationrecovery.ShouldInjectRecovery(converterscore.TruncationRecoveryEnabled)`. Al ser default-on, omitir este cableado en Task 10 rompería el recovery silenciosamente (misma clase de riesgo que el Critical de Task 4). `EmitError` — sin equivalente-método en el original, que hace `yield format_sse_event("error", ...)` inline en el `except Exception` del generador — expone ese mismo evento para que el caller (fuera de este paquete: la capa HTTP que consume el stream de Kiro) lo dispare cuando la lectura falla a mitad de stream; `FirstTokenTimeoutError` NO dispara este evento (streaming_anthropic.py:695-696, re-raise silencioso). No se porta la intercepción MCP de `web_search` (streaming_anthropic.py:354-468, Path B): requiere una llamada de red real a `kiro.mcp_tools.call_kiro_mcp_api`, pertenece a una capa de ejecución de herramientas fuera del alcance de un formatter SSE, y ningún fixture de `stream_kiro_to_anthropic` la ejercita (el único fixture con una tool `web_search` en `request_tools`, `d3fa86d2d3539b88`, solo emite un evento "content", nunca un "tool_use" nombrado "web_search" — se prueba la estimación de tokens de la definición de la tool, no la intercepción). Corpus: `format_sse_event/` 83/83 (`formatter_test.go`, opción (a) del brief: se testea `internal/sse.FormatEvent` + `internal/pyjson.Dumps` directamente contra estos fixtures, sin pasar por `Formatter`, porque no dependen de ningún estado generado — comparación byte a byte, no hay message-id ni nada no determinista en esta capa). `stream_kiro_to_anthropic/` 16/20 pass + 4/20 skip documentado (0 fail) — ver ledger abajo. **Ruling — comparación por forma (`message_id`)**: igual que Task 7's `chatcmpl-<32hex>`. El corpus se grabó con `uuid.uuid4()` monkeypatcheado a un contador global incremental por sesión de test (`internal/utils/ids.go`'s "Contrato de congelación"), así que cada fixture trae el valor que tocaba en SU posición del contador global — no reproducible sin recrear el orden exacto de ejecución de toda la suite Python. `formatter_test.go` verifica el id por regex `^msg_[0-9a-f]{24}$` en ambos lados (actual y esperado) y lo normaliza antes de comparar el resto del payload por igualdad estructural. **Ruling — cross-package tokencount**: movidas a `internal/tokenizer/requesttokens.go` (exportadas: `CountTokens`, `CountMessageTokens`, `CountToolsTokens`, `CountSystemTokens` nueva), no aliased ni reimplementadas. Se descartó (a) exportar directamente desde `internal/streamingopenai` porque un formatter de un dialecto dependiendo del paquete interno de otro dialecto es una capa más rara que un paquete `tokenizer` compartido, y el brief lista `internal/tokenizer` como paquete consumido por Task 8; (b) reimplementar desde cero en `streaminganthropic` porque duplicaría ~250 líneas ya probadas contra corpus por Task 7. `internal/streamingopenai/tokencount.go` quedó con solo `isJSONTruthy` (dialect-specific, gate de "usage event recibido"); `formatter.go` de ese paquete se actualizó para llamar `tokenizer.CountTokens`/`CountMessageTokens`/`CountToolsTokens` — comportamiento idéntico, confirmado corriendo su test suite antes/después del refactor (mismo resultado: 39/40 + el fixture ahora explícitamente skipeado). **Ruling — 4 fixtures skipeados con prueba** (misma clase que el caso de monkey-patch de Task 7): `b0f2a0cabe68929e`, `e21af20711a4832e`, `f0f3c8b21b133a12` tienen `estimate_request_tokens` mockeado con `return_value={"total_tokens": N}` fijo en el test Python de origen (`test_streaming_anthropic.py:1105,1171,1139` respectivamente — confirmado por grep), desconectado del contenido real de `request_messages`/`request_tools`/`request_system`; la implementación real de este port (`tokenizer.CountMessageTokens`+`CountToolsTokens`+`CountSystemTokens`) computa 11/9/26 respectivamente para esos mismos inputs — valores distintos por construcción, no por bug. `51e4de551161d0ff` tiene `parse_bracket_tool_calls` mockeado a un resultado no vacío inventado por el test (`test_streaming_anthropic.py:463`) para contenido `"[tool_call: func1]"`, que NO matchea el patrón real `\[Called\s+(\w+)\s+with\s+args:\s*` (`internal/parsers.ParseBracketToolCalls`, ya portado 1:1 en Task 1); TODOS los demás tests de `test_streaming_anthropic.py` mockean esa misma función a `return_value=[]`, confirmando que la función real nunca se ejercita a través de este corpus. Los 4 casos están marcados con `t.Skip(...)` con cita de línea en `formatter_test.go`, no como fallos silenciosos.) |
| `routes_openai.py` | `internal/routesopenai` | `handler.go`, `failover.go`, `stream.go`, `collect.go`, `handler_test.go` (Task 9 porta `GET /v1/models` y `POST /v1/chat/completions` en la rama account-system de `routes_openai.py:122-556` — la rama "legacy" de una sola cuenta sin failover, `routes_openai.py:558-769`, no se porta: este servicio siempre corre con `accountmanager.Manager`. `handler.go`: `Handler`/`New`, `Models`, `thinkingHandling` (mapea `FAKE_REASONING_HANDLING` a `streamingopenai.ThinkingHandling`), el sobre de error unificado `writeOpenAIError`. `failover.go`: `ChatCompletions` (decodifica el body), `failoverChatCompletions` (bucle `exclude`+`GetNextAccount` sin contador `MAX_ATTEMPTS` explícito — confía en que `GetNextAccount` ya agota vía cuarentena+exclude), `attemptAccount`, `handleKiroError`/`handleTransportError` (clasificación Fatal/Recoverable), `writeExhausted`. `stream.go`/`collect.go`: el bucle explícito D4 (`drivePipeline`, sin goroutines) que alimenta `streamingcore.Pipeline`→`streamingopenai.Formatter`; el modo no-streaming (`collectResponse`, port de `collect_stream_response`, streaming_openai.py:576-688) NO lee los acumuladores internos del Formatter — re-parsea los mismos bytes SSE que produciría el modo streaming, igual que hace el original. **Cuatro desviaciones documentadas frente al brief/upstream** (razonadas en el comentario de cabecera de `handler.go`): (1) `owned_by` en `/v1/models` es `"anthropic"` (routes_openai.py:151), no `"kiro"` como decía el brief — upstream gana; (2) dialecto de error unificado `{"error":{"message","type":"kiro_api_error","code"}}` para TODAS las respuestas de error de esta ruta (incluida cuenta-agotada), no solo para las JSONResponse explícitas del original — sigue el spec §7.1, más consistente para un cliente OpenAI; (3) Kiro se llama SIEMPRE con `stream=true` en `RequestWithRetry` (routes_openai.py:358-363/612-617), independientemente de `request_data.stream`, que solo decide el formato de la respuesta al cliente; (4) fallos de transporte (`*httpclient.RequestError`) se tratan siempre como Recoverable en el failover, sin pasar por `accounterrors.Classify` (que trataría cualquier `SuggestedHTTPCode` 5xx como Fatal) — replica el override explícito de `ErrorType.RECOVERABLE` de routes_openai.py:512-517 para este caso, ya que `accountmanager.Manager.ReportFailure` no admite forzar la clasificación. **Seam de test**: `Handler.apiURL` (campo no exportado, `handler.go`) permite a `handler_test.go` apuntar al `httptest.Server` falso — `auth.Manager.APIHost()` siempre devuelve un host real bajo `*.kiro.dev` sin override público, mismo patrón que `accountmanager.Manager.listURLOverride`. 6/6 escenarios del brief cubiertos en `handler_test.go`, con dos ajustes deliberados: el escenario de failover usa 402 en vez de 500 (un 500 real de Kiro es Fatal según la tabla de `accounterrors`, no dispararía failover) y el escenario de cuenta agotada usa dos cuentas con 402 constante en vez de un `500`x3 literal, por la misma razón.) |
| `routes_anthropic.py` | `internal/routesanthropic` | `handler.go`, `failover.go`, `stream.go`, `collect.go`, `counttokens.go`, `handler_test.go`, `helpers_test.go` (Task 10 porta `POST /v1/messages` y `POST /v1/messages/count_tokens` de la rama account-system de `routes_anthropic.py:119-959`. Es el gemelo de `internal/routesopenai` para el dialecto Anthropic: comparte EXACTAMENTE la misma forma de bucle de failover (`exclude`+`GetNextAccount` sin `MAX_ATTEMPTS`), manejo de errores de transporte (`handleTransportError` → `ReportFailureAs(..., accounterrors.Recoverable, ...)`, mismo override que Task 9), y bucle explícito D4 (`drivePipeline`, sin goroutines). Diferencias del dialecto: (a) builder de payload `convertersanthropic.AnthropicToKiro`; (b) formatter `streaminganthropic` con block indexing — `drivePipeline` emite PRIMERO `message_start` (`EmitMessageStart`, el primer yield del generador, streaming_anthropic.py:206-223) antes del bucle de eventos, y el stream termina con `message_delta`+`message_stop` vía `Finish`, NO con `data: [DONE]` (a diferencia de OpenAI); (c) el system prompt llega en su campo propio de la petición (`req.System`), no dentro de la lista de mensajes; (d) sobre de error del dialecto Anthropic `{"type":"error","error":{"type":<t>,"message":<m>}}` — `api_error` para agotamiento/errores de Kiro/internos, `invalid_request_error` para cuerpos mal formados (routes_anthropic.py:390-398). `stream.go` fija además `Cache-Control: no-cache` y `Connection: keep-alive` (routes_anthropic.py:489-496), que el dialecto OpenAI no pone. `collect.go` (`collectResponse`) re-parsea los bytes SSE que `drivePipeline` ya escribió para reconstruir la respuesta única `message` (port de `collect_anthropic_response`, streaming_anthropic.py:721-863), mismo diseño que routesopenai — streaming y no-streaming nunca discrepan. **Fix round 1**: `serveNonStreaming` sobreescribe `usage.input_tokens` con el valor derivado de context_usage cuando Kiro lo reportó (el caso común, ~39/40 fixtures) vía el nuevo `streaminganthropic.Formatter.ContextCorrectedInputTokens()` — `collect_anthropic_response` usa ese número informado por Kiro (`calculate_tokens_from_context_usage`, streaming_core.py:337-362: `int(pct/100*200000)-output_tokens`), no la estimación pre-petición del message_start (streaming_anthropic.py:809-816). El SSE streaming se queda con la estimación a propósito: el protocolo Anthropic fija input_tokens en message_start y no tiene campo posterior para reenviarlo (límite de upstream, no un bug del port). El campo numérico `contextUsagePercentage` se añade al `Formatter` de Task 8 para esto (antes solo guardaba un bool). `counttokens.go` (`CountTokens`) es PURAMENTE LOCAL (no toca Kiro ni `GetNextAccount`): replica `estimate_request_tokens(apply_claude_correction=True)` (routes_anthropic.py:948-952) sumando TRES sub-conteos corregidos individualmente — `tokenizer.CountMessageTokens(m,true)+CountToolsTokens(t,true)+CountSystemTokens(s,true)` — no una sola corrección al total (ver la fila `tokenizer.py`). **`thinkingHandling`**: config `FAKE_REASONING_HANDLING` ∈ {as_reasoning_content, remove, pass, strip_tags} (config.py:463-467); `as_reasoning_content` → `streaminganthropic.AsReasoningContent`, todo lo demás → `Strip` (el `else` de streaming_anthropic.py:265-324; su rama `include_as_text` es código muerto — ese valor nunca pasa la validación de config; remove/pass/strip_tags ya resuelven el thinking en el Pipeline antes de llegar al formatter). Difiere de routesopenai, cuyo `else` es `AsContent` porque streaming_openai.py:163 SÍ incluye el thinking como texto en su else, no lo descarta. **Desviaciones documentadas** (cabecera de `handler.go`): mismas 1-3 que routesopenai (sobre unificado, Kiro siempre `stream=true`, transporte siempre Recoverable) más (4) error a MITAD de stream NO emite el `event: error` de routes_anthropic.py:465-469 — igual que routesopenai.serveStreaming descarta el error de `drivePipeline` una vez arrancado el SSE (fallo probable = desconexión del cliente); los errores pre-stream sí emiten el JSON Anthropic correcto; y (5) web_search Path A/B (routes_anthropic.py:262-310,354-468) NO se porta — intercepta tools server-side llamando a una API MCP de red real, fuera del alcance de la capa de rutas, misma decisión que Task 8 para el formatter. **Seam de test** `Handler.apiURL` idéntico a routesopenai. 6 escenarios de `/v1/messages` (streaming, no-streaming, failover, agotamiento 503, Fatal de cuenta única, fallo de transporte + breaker armado) + 1 de `count_tokens` (valida el cableado contra el cómputo directo del tokenizer y que la corrección 1.15 esté aplicada) en `handler_test.go`/`helpers_test.go`.) |
| `http_client.py` | `internal/httpclient` | `transport.go`, `proxy.go`, `client.go`, `stream.go` |
| `auth.py` | `internal/auth` | `types.go`, `manager.go`, `json_source.go`, `oidc.go`, `sqlite.go`, `refresh.go` (Task 1-2 porta `types.go`, `manager.go` (constructores + `AccessToken`/`ProfileARN`, stub de `ForceRefresh`), `json_source.go`, stubs de `oidc.go`/`sqlite.go`. Task 3 completa `oidc.go` con `refreshAWSSSO`/`doAWSSSORefreshAttempt` (ports de `auth.py:743-869`) y `loadEnterpriseDeviceRegistration` (port de `auth.py:458-487`). Task 4 completa `sqlite.go` (`loadFromSQLite`/`saveToSQLite` — ports de `auth.py:248-382` y `auth.py:388-633`). Task 5 implanta `refresh.go` con `Refresh()` singleflight (port de `auth.py:870-934` `get_access_token`), graceful degradation (SQLite+400, `auth.py:906-919`), y reestructura `AccessToken`/`ForceRefresh`) |
| `account_manager.py` | `internal/accountmanager` | `types.go`, `discovery.go`, `state.go`, `manager.go`, `manager_test.go`, `breaker.go`, `selection.go`, `failover.go`, `failover_test.go`, `init.go`, `init_test.go`, `integration_test.go`, `reportfailureas_test.go` (Task 6 porta `types.go` (Account, AccountStats, ModelAccountList), `discovery.go` (loadCredentials, processRefreshTokenEntry, processFileEntry, scanDirectory, processFile, isValidJSONFile, isValidSQLiteFile — ports de `account_manager.py:127-326`), `state.go` (LoadState, SaveState, renameWithRetry — ports de `account_manager.py:328-416`), `manager.go` (Manager struct, NewManager, LoadCredentials, SaveStatePeriodically, hasStateChanged, Accounts — ports de `account_manager.py:179-214` y las rutas del state loop). Task 7 implanta circuit breaker (breaker.go — quarantineWindow, isInQuarantine), sticky selection (selection.go — nextEnabledIdx), failover (failover.go — GetNextAccount, ReportSuccess, ReportFailure — todos ports de `account_manager.py:645-867`). Task 8 implanta model catalog (init.go — Initialize, refreshAccountModels, GetAllAvailableModels, GetFirstAccount, fallbackModels — ports de `account_manager.py:432-644, 868-897` y config.py:276-290). Task 9 implanta integration test (`integration_test.go` — TestPhase4Integration que verifica wiring de Manager + auth.Manager + httpclient.Client contra fake Kiro server: 200 success, 429 retry, 500 failover). Task 9 fix round 1 (routesopenai) añade `ReportFailureAs(accountID, model string, kind accounterrors.Type, statusCode int, reason, msg string) accounterrors.Type` (failover.go): el original `report_failure` toma `error_type` como parámetro del LLAMADOR (`account_manager.py:809-816`), no algo que derive internamente — `routes_openai.py:513-516` pasa `ErrorType.RECOVERABLE` para un fallo de TRANSPORTE 502/504 aunque `classify_error(502-o-504, None)` lo llamaría Fatal por su cuenta (la tabla de `account_errors.py` trata cualquier 5xx como Fatal por defecto — correcto para una respuesta HTTP real de Kiro, no para un fallo de transporte). La `ReportFailure` de fase 4 auto-clasificaba vía `accounterrors.Classify` sin ningún hueco para que el llamador forzara la clasificación, así que no podía replicar ese caso; ahora `ReportFailure` se refactoriza para delegar en `ReportFailureAs(accountID, model, accounterrors.Classify(statusCode, reason), statusCode, reason, msg)` — mismo comportamiento observable, verificado en `reportfailureas_test.go` (`TestReportFailureDelegatesToReportFailureAs`) además de por la suite existente de `failover_test.go`, que no cambia. `TestReportFailureAsForcesClassification` fija el contrato nuevo: `ReportFailureAs(id, model, accounterrors.Recoverable, 500, "", msg)` arma el circuit breaker (incrementa `ConsecutiveFailures`, la cuenta queda excluida en el siguiente `GetNextAccount`) pese a que `Classify(500, "")` por sí solo daría Fatal.) |
| `mcp_tools.py` | `internal/mcptools` | `ids.go`, `client.go`, `summary.go`, `websearch.go`, `sse.go`, `sseopenai.go`, `ids_test.go`, `client_test.go`, `summary_test.go`, `websearch_test.go`, `sse_test.go`, `handle_test.go` (Task 6 fase 6a porta el cliente MCP de Kiro para web_search y su emulación SSE — mcp_tools.py:1-753, spec §6.14. Alcance de esta tarea: SOLO el paquete `internal/mcptools` (cliente + generadores + `HandleNativeWebSearch`); el cableado en `internal/routesopenai`/`internal/routesanthropic` (Path A: tools nativas `type` que empieza por `web_search`; Path B: auto-inyección si `WEB_SEARCH_ENABLED`) es la siguiente tarea de esta fase — ninguno de esos dos paquetes se toca aquí. **`ids.go`**: los 3 patrones de id, construidos SOLO a partir del seam `utils.NewHexToken` (sin fuente de aleatoriedad nueva): `NewToolUseID` = `"srvtoolu_" + utils.NewHexToken()` (mcp_tools.py:126); `NewWebSearchRequestID` = `web_search_tooluse_<22hex>_<timestamp ms>_<8hex>` (mcp_tools.py:121-125) — diverge de forma DELIBERADA del alfabeto base62 de `generate_random_id` (mcp_tools.py:56-70, `random.choices(ascii_letters+digits)`) a hex, porque los ids se comparan por FORMA (regex) en los tests, nunca por valor, y el brief pide expresamente no introducir un `math/rand` nuevo; `msg_<24hex>` no tiene wrapper propio — se reusa `utils.GenerateMessageID()` directo en `sse.go`/`websearch.go`. **`client.go`** (`CallKiroMCPAPI(ctx, host, query, tp) (toolUseID, results, err)`): POST `{host}/mcp` JSON-RPC 2.0 (`tools/call`/`web_search`), timeout 60s fijo. **Doble deserialización** (escenario nombrado del brief): `result.content[0].text` es una CADENA que a su vez contiene JSON (mcp_tools.py:119,184-186) — se deserializa la envoltura JSON-RPC y LUEGO ese string, por separado; cubierto por `TestCallKiroMCPAPI_DoubleDeserialize`. **Dos desviaciones de firma/implementación razonadas en el comentario de cabecera del archivo**: (1) `host string` se añade como parámetro explícito — `utils.TokenProvider` (utils/headers.go:17) no expone el q_host, y ensancharla rompería el seam compartido con el resto del port; el llamador (Task 7) lo obtiene de `auth.Manager.QHost()`. (2) Las cabeceras del POST se construyen A MANO desde `tp.AccessToken(ctx)` — NO vía `utils.GetKiroHeaders` — porque esa función es el port de `utils.py::get_kiro_headers`, que en el ORIGINAL solo usan `http_client.py` y `account_manager.py` (confirmado por grep) y produce las 9 cabeceras de `GenerateAssistantResponse` (`Content-Type: application/x-amz-json-1.0`, `x-amz-target` fijo, `x-amzn-codewhisperer-optout: "true"`); `mcp_tools.py:151-155` nunca la importa — construye su propio dict de 3 cabeceras inline con `Content-Type: application/json` y `x-amzn-codewhisperer-optout: "false"` (literalmente el valor opuesto). Reusar `GetKiroHeaders` aquí habría mandado cabeceras incorrectas a `/mcp`; se prioriza la fidelidad al original sobre la lectura literal del brief de "Interfaces consumidas", que nombraba `GetKiroHeaders` sin conocer este detalle. **Cliente HTTP**: `net/http` directo con `&http.Client{Timeout: 60s}` nuevo por llamada — NO `internal/httpclient.Client.RequestWithRetry` — porque `call_kiro_mcp_api` en el original NUNCA pasa por el `KiroHttpClient` compartido: abre un `httpx.AsyncClient(timeout=60.0)` efímero y hace UN solo POST sin reintentos (ni 403→refresh, ni 429/5xx→backoff), a diferencia de `RequestWithRetry`, que SÍ implementa ambos — usarlo aquí introduciría reintentos que el original nunca tuvo para este endpoint. **`summary.go`** (`GenerateSearchSummary(query, results) string`, mcp_tools.py:205-274): envuelve en `<web_search>...</web_search>`, snippet COMPLETO sin truncar; replica la rareza `if results and "results" in results` (mcp_tools.py:237) donde un dict no vacío con `"results": []` produce cabecera+cierre sin "No results found." (cubierto por `TestGenerateSearchSummary_EmptyResultsListNoFallbackMessage`); default de título "Untitled" solo aquí (`.get("title","Untitled")`) — los constructores de `web_search_tool_result` en `sse.go`/`websearch.go` usan default `""` (`.get("title","")`, mcp_tools.py:369,717), divergencia real entre los dos call sites que SE REPLICA, no se unifica. **`sse.go`/`sseopenai.go`** (`GenerateAnthropicWebSearchSSE`/`GenerateOpenAIWebSearchSSE`, mcp_tools.py:281-527, separados en dos archivos solo por tamaño — `sse.go` rozaba las 400 líneas del plan, ambos comparten `chunkRunes`/`outputTokenCount`/`nowUnix` de `sse.go`): 11 eventos Anthropic vía `internal/sse.FormatEvent`/el dialecto OpenAI sin `event:` + `internal/sse.FormatDone()` al final; troceo de summary en fragmentos de 100 RUNAS (no bytes — `chunkRunes`, Python trocea sobre code points) para las deltas de texto. A diferencia del generador async del original (que yield-ea a un `StreamingResponse` en vivo evento a evento), estas dos funciones devuelven el stream SSE COMPLETO ya enmarcado en un solo `[]byte`: no hay ninguna fuente incremental real que justifique escribir a un `io.Writer` con flush entre eventos (todo el contenido —`results`, el summary— ya está entero en memoria cuando arrancan). **Fix round 1 (parity #1)**: SÍ usa `internal/pyjson.Dumps` para los `data:` de ambos generadores (`writeAnthropicEvent` en `sse.go`, `writeOpenAIChunk` en `sseopenai.go`), igual patrón que `streaminganthropic/payloads.go::writeEvent` — `encoding/json.Marshal` a secas diverge de `json.dumps(..., ensure_ascii=False)` (lo que usa `format_sse_event`, streaming_anthropic.py:70-85, y lo que pasan explícitamente los tres `json.dumps(chunk, ensure_ascii=False)` de mcp_tools.py:488,505,524) en separadores SIN espacio y en escapar `&`/`<`/`>` a sus secuencias \u00XX por su modo HTML-safe por defecto — una divergencia de bytes real y frecuente para web_search en concreto (URLs con `&`, títulos/snippets con `<`/`>`/`&`), no solo cosmética; cubierto por `TestGenerate{Anthropic,OpenAI}WebSearchSSE_AmpersandNotEscaped`. Excepción deliberada: el `partial_json` interno del evento `content_block_delta` (mcp_tools.py:354, `json.dumps({"query": query})` SIN `ensure_ascii=False`) usa `pyjson.DumpsASCII`, no `pyjson.Dumps` — sigue la regla que el propio paquete `pyjson` documenta (`Dumps` solo para call sites con `ensure_ascii=False` explícito). El request MCP saliente (`client.go`, hacia `{host}/mcp`) NO pasa por `pyjson`: sigue con `encoding/json.Marshal` a secas porque el original tampoco le aplica `ensure_ascii=False` a esa llamada (es un POST JSON-RPC corriente, no un `json.dumps` con esa bandera). **`websearch.go`** (`ExtractQueryFromMessages(messages, apiFormat) (string, bool)`, mcp_tools.py:534-587): lee el PRIMER mensaje, contenido string u OpenAI/Anthropic block-list indistintamente — `apiFormat` se acepta sin usarse, igual que el original (mcp_tools.py:534 lo declara y jamás lo referencia en el cuerpo); retira el prefijo `"Perform a web search for the query: "` si está presente. `HandleNativeWebSearch(ctx, req, host, tp, apiFormat) NativeWebSearchOutcome` (mcp_tools.py:590-753) es la ADICIÓN de esta tarea sobre el brief mínimo — el brief pedía "una firma limpia que Task 7 pueda llamar" sin fijarla; `NativeWebSearchRequest{Messages, Model, Stream}`/`NativeWebSearchOutcome{StatusCode, Streaming, Body}` desacoplan este paquete de los tipos concretos de petición/respuesta de `routesopenai`/`routesanthropic` (fuera del alcance de esta tarea). Replica la extracción de query → llamada MCP → SSE o JSON según `req.Stream`, con los cuerpos de error 400/500 SIEMPRE en forma Anthropic (`{"type":"error","error":{...}}`) incluso cuando `apiFormat=="openai"` — el original NO ramifica esos dos `JSONResponse` por formato (mcp_tools.py:613-623,630-640), se replica tal cual, no es un bug del port. Consume `internal/tokenizer.CountMessageTokens`/`CountTokens` (no listado en las "Interfaces consumidas" del brief, pero requerido: el original llama `count_message_tokens`/`count_tokens` en este mismo punto, mcp_tools.py:643-646,680-681, y el paquete ya existe con exactamente esas firmas desde fase 6a Task 8). **Tests** (`go test ./internal/mcptools/...`, 39/39 verde — 34 originales + 3 del fix round 1 Minor #2 + 2 del fix round 1 parity #1): doble-deserialización + status≠200 + error JSON-RPC + JSON interno inválido + content presente-vacío + text presente-vacío (ambos deben FALLAR, fix round 1) + content/text realmente ausentes (deben tener éxito con resultado vacío) + fallo de `AccessToken` (`client_test.go`, `httptest` fake, sin red real); wrap+formato+snippet completo+troceo 100 runas Unicode-safe+rareza results-vacío+default Untitled (`summary_test.go`); ambos formatos de mensaje + prefijo + concatenación multi-bloque + 5 casos ok=false (`websearch_test.go`); orden de 11 eventos Anthropic + reensamblado de deltas + ausencia de `[DONE]` + formato OpenAI sin `event:` con `[DONE]` + `&`/`<`/`>` sin escapar en ambos dialectos (`sse_test.go`); los 3 patrones de id por regex (`ids_test.go`); `HandleNativeWebSearch` extremo a extremo con fake MCP server — 400/500/streaming×2 formatos/no-streaming×2 formatos (`handle_test.go`). Ids comparados por FORMA (`srvtoolu_[0-9a-f]{32}`, `msg_[0-9a-f]{24}`, `web_search_tooluse_[0-9a-f]{22}_\d+_[0-9a-f]{8}`), nunca por valor — mismo criterio que `chatcmpl-`/`call_`/`msg_` del resto del port.) |
| `debug_logger.py` | `internal/debuglogger` | `debuglogger.go`, `debuglogger_test.go` (Task 4 fase 6a. `Mode` (`ModeOff`/`ModeErrors`/`ModeAll`), `New(mode, dir)` sin el singleton `__new__` del original — cada llamada devuelve una instancia nueva que el llamador comparte, como `cache.ModelInfoCache`; el paquete no importa `internal/config` ni usa `os.Getenv` (config por parámetro). Métodos del brief portados 1:1: `LogRequestBody`/`LogKiroRequestBody` (`:156-186`), `LogRawChunk`/`LogModifiedChunk` (`:188-218`, factorizados internamente vía `logBody`/`logChunk` — mismo patrón write-inmediato-en-"all"/buffer-en-"errors"), `LogErrorInfo` (`:220-249`), `FlushOnError` (`:251-316`), `DiscardBuffers` (`:318-330`). **Nuance verificada+testeada**: en modo "errors", `FlushOnError` con los 4 buffers vacíos es un no-op COMPLETO — ni crea el directorio ni escribe `error_info.json` (`:272-279`, `if not any([...]): return`, ANTES del try/finally) — cubierto por `TestModeErrorsFlushOnErrorNoDataIsNoop`. **Adición sobre el brief (no listada en su interfaz)**: `PrepareNewRequest(ctx) context.Context`, port de `prepare_new_request` (`:129-154`), necesaria porque es el único punto del original que crea el buffer de logs de aplicación por petición (`debug_middleware.py:98`, llamado ANTES de leer el cuerpo) — sin ella, Task 5 (`debugmiddleware`) no tendría cómo instalar "un buffer fresco en el context de la petición" tal como pide el contexto de esa tarea. Devuelve un `context.Context` derivado con el buffer de logs instalado (clave de context no exportada, `logBufferCtxKey`), en vez de mutar un campo global como hace el sink de loguru del original. **`Handler() slog.Handler`**: replica el formato exacto `{YYYY-MM-DD HH:mm:ss.SSS} \| {LEVEL:<8} \| {origen}:{función}:{línea} \| {mensaje}` (`:121-127`, §6.13) vía `runtime.CallersFrames` sobre `slog.Record.PC` (paquete corto Go en vez del módulo Python con puntos — no se persigue paridad de bytes con logs reales de Python, solo la FORMA de la línea, verificado por regex en `TestHandlerFormatsLineExactly`). `Enabled`/`Handle` solo actúan si el `context.Context` de la llamada lleva el buffer que `PrepareNewRequest` instaló — activación por-context en vez de por-sink-global, así que dos peticiones concurrentes con su propio `PrepareNewRequest` no mezclan logs (`TestHandlerMultipleRequestsDoNotMixBuffers`), a diferencia del sink único global del original bajo concurrencia real. `WithAttrs`/`WithGroup` son no-ops deliberados: el formato loguru original solo interpola `{message}`, sin atributos estructurados ni grupos. **Volcado JSON** (`writeJSONOrRaw`, para `request_body.json`/`kiro_request_body.json`): a diferencia del original (`json.loads`+`json.dump(indent=2, ensure_ascii=False)`, que decodifica y reserializa), este port solo re-indenta los bytes ya válidos con `encoding/json.Indent` sin decodificar — evita reconstruir a mano el orden de claves (los mapas de Go no lo preservan) y basta para un volcado de depuración; si el body no es JSON válido, se escriben los bytes crudos, igual que el original. `error_info.json` usa un struct (`errorInfo{StatusCode, ErrorMessage}`) en vez de `map[string]any` para preservar el orden de campos del dict original sin depender del orden alfabético que aplicaría `encoding/json` a un mapa. **Restricción de tamaño**: el fichero quedó en 397 líneas (límite <400 del plan) tras dos rondas de compresión de comentarios — los métodos `Log*`/helpers de escritura se factorizaron (`logBody`/`logChunk`/`writeIfNonEmpty`/`logErr`) para reducir duplicación y ayudar a entrar en el límite, no solo por brevedad de comentarios.) |
| `debug_middleware.py` | `internal/debugmiddleware` | `middleware.go`, `middleware_test.go` (Task 5 fase 6a. `New(cfg *config.Config) func(http.Handler) http.Handler` construye UN `*debuglogger.DebugLogger` compartido dentro del closure devuelto (single-flight, replicando el singleton de proceso `debug_logger` del original — `debug_logger.py:56-60` — pero closure-captured, no global mutable de paquete). **Ruling del controlador de esta tarea**: se monta como middleware GLOBAL en `internal/server.Server.buildHandler` (no como wrapper por-ruta del mux), igual que el `BaseHTTPMiddleware` de Starlette del original (también global, auto-limitado mirando `request.url.path`) — orden final `cors → debugmiddleware.New(cfg) → auth → recover → mux`, es decir, el middleware corre ANTES de auth (replica la propiedad "antes de validar" del original para 401 también, no solo 422) y dentro de CORS. `inScope` replica `LOGGED_ENDPOINTS` (`:47-50`, `POST /v1/chat/completions` y `POST /v1/messages` únicamente — exige POST explícitamente, ausente en el original porque Starlette ya solo registra ese verbo en esas rutas) y el guard de `DEBUG_MODE=="off"` (`:86-87`): fuera de esas 2 rutas, o con `dispatch` no llamado, el request pasa intacto. Para las 2 rutas en scope, el logger SIEMPRE se inyecta en el context (`context.WithValue`, clave no exportada `loggerCtxKey`, recuperable vía `FromContext(ctx) *debuglogger.DebugLogger`) incluso en modo `off` (inerte: ningún método de `DebugLogger` hace nada en ese modo) — solo cuando el modo no es `off` se llama además a `logger.PrepareNewRequest(ctx)` (instala el buffer de logs de aplicación de la petición) y se lee el cuerpo completo para `logger.LogRequestBody(body)` (`if body:`, `:104`, solo si no está vacío), ANTES de pasar al siguiente handler — igual que `debug_logger.prepare_new_request()` + `request.body()` + `log_request_body(body)` corren antes de la validación Pydantic en el original (`:96-108`). **Re-wrap del cuerpo** (necesario porque `net/http.Request.Body` es un stream de un solo uso, a diferencia de `request.body()` de Starlette que cachea el resultado, `:100-101`): tras leer, `r.Body` se repone con `io.NopCloser(bytes.NewReader(body))` para que el handler downstream (incluida la validación que produce un 422) pueda leer el cuerpo completo de nuevo — verificado por `TestChatCompletions_InvalidBody_StillPreparesLoggerBeforeValidation`, que fuerza un cuerpo JSON inválido, comprueba que el handler downstream ve los mismos bytes y que `request_body.json` se escribió en `DEBUG_DIR` (modo `all`, escritura inmediata) antes de que ese handler simulado devuelva 422. **Fuera de scope de esta tarea (YAGNI, según el brief)**: NO se cablea `FlushOnError`/`DiscardBuffers` en ningún handler de ruta — eso es responsabilidad de los handlers/exception-handlers en el original (`:110-113`, comentario de cabecera `:32-35`) y de tareas posteriores de esta fase; este middleware solo prepara y loguea el cuerpo crudo. **Montaje** (`internal/server/server.go`, `buildHandler`): `h = recoverMiddleware(h); h = authMiddleware(...); h = debugmiddleware.New(s.cfg)(h); h = corsMiddleware(h)` — debug ENTRE auth y CORS, de modo que la petición lo atraviesa antes que auth.) |
| `__init__.py` | *(sin equivalente: solo reexporta)* | |

## Duplicación temporal: `model_resolver.py`

`internal/convertersanthropic/converters.go` (Task 9) porta, como funciones NO
exportadas (`normalizeModelName`, `getModelIDForKiro`) más el package var
`HiddenModels`, el subconjunto mínimo de `model_resolver.py` que
`anthropic_to_kiro` necesita para calcular el `modelId` que manda a Kiro
(`normalize_model_name` + `get_model_id_for_kiro`, sin la clase
`ModelResolver` completa, sin caché dinámica ni alias — el propio original
describe `get_model_id_for_kiro` como "a simple helper for converters that
don't have access to the full ModelResolver"). Se hizo así porque
`internal/modelresolver` (fila de arriba) todavía no existe y Task 9 no puede
crear paquetes fuera de `internal/convertersanthropic/*`. Cuando una tarea
futura implemente `internal/modelresolver`, debería sustituir ese subconjunto
en `converters.go` por una llamada real a ese paquete en vez de mantener dos
copias del mismo algoritmo.

`internal/convertersopenai/converters.go` (Task 10) repite exactamente la
misma duplicación, por la misma razón y con la misma restricción de alcance
(Task 10 solo puede tocar `internal/convertersopenai/*` y este fichero):
`build_kiro_payload` del original también llama a
`get_model_id_for_kiro(request_data.model, HIDDEN_MODELS)`. En vez de
importar `convertersanthropic` (sus dos funciones no están exportadas, y
acoplar un adaptador a los internals de otro no reduce el radio de impacto),
este paquete lleva su propia copia de `normalizeModelName` +
`getModelIDForKiro` + `HiddenModels`. Cuando `internal/modelresolver` exista,
la tarea que lo cree debería sustituir las TRES copias (converterscore no
tiene una propia — solo los dos adaptadores) por una llamada real a ese
paquete.

## Paquetes que no existen en el original

| Paquete Go | Responsabilidad | Por qué existe |
|---|---|---|
| `internal/sse` | Formateo de eventos SSE (Task 5) | Rompe el ciclo `mcp_tools` ↔ `streaming_anthropic` (FormatEvent con dialecto OpenAI/Anthropic, FormatDone OpenAI-only) |
| `internal/pyjson` | Emula `json.dumps` y `str()` de Python | Aísla las rarezas de Python en un solo sitio |
| `internal/testutil` | Cargador del corpus y generadores de chunks | Equivalente de los helpers de `conftest.py` |
| `internal/server` | Router, middleware y ciclo de vida | Equivalente del `lifespan` de FastAPI |
| `internal/version` | Cadena de versión | En Python es una constante en `config.py` |

## Renombrados

| Python | Go | Motivo |
|---|---|---|
| `exceptions.py` | `internal/validationerrors` | «exceptions» no significa nada en Go |

## Ciclos de importación rotos

| Ciclo en Python | Cómo se rompe |
|---|---|
| `utils` ↔ `auth` | `utils` declara la interfaz `TokenProvider` y `auth.Manager` la satisface |
| `mcp_tools` ↔ `streaming_anthropic` | El formateo de SSE se extrae a `internal/sse` |

## Extensiones deliberadas respecto al original

| Dónde | Qué | Por qué |
|---|---|---|
| `internal/auth.AuthType` | 5 valores (`Unknown`, `KiroDesktop`, `AWSSSO`, `KiroCLI`, `RefreshOnly`) en vez de los 2 del `Enum` original (`KIRO_DESKTOP`, `AWS_SSO_OIDC`, `auth.py:68-83`) | Reflejan los tres `type` que ya distingue `credentials.json` (`json`, `sqlite`, `refresh_token`, spec §6.11) en vez de que kiro-cli y refresh-token-only colapsen en `KIRO_DESKTOP` como hoy hace el Python |
| `internal/auth` (JSON source) | La detección de región por ARN (`auth.py:357-366`, solo en el loader de SQLite en el original) se generaliza como fallback también para el JSON de Kiro Desktop | Pedido explícitamente por el plan de la Task 2 (4 niveles de precedencia de región, incluido "detectado del ARN") y reutilizable sin cambios por la Task 4 (SQLite) |
