# Correspondencia entre el original Python y este port

Origen: `jwadow/kiro-gateway`, v2.4.dev.13, commit `a5292ca`.

Regla: el nombre del paquete Go es el del módulo Python sin guiones bajos. La columna
`Ficheros Go` se rellena a medida que cada fase implementa su paquete.

| Módulo Python | Paquete Go | Ficheros Go |
|---|---|---|
| `main.py` | `cmd/kiro-gateway` + `internal/server` | `cmd/kiro-gateway/main.go` |
| `config.py` | `internal/config` | `config.go`, `parse.go`, `dotenv.go` |
| `utils.py` | `internal/utils` | `fingerprint.go`, `ids.go`, `headers.go` |
| `tokenizer.py` | `internal/tokenizer` | `bpe.go`, `pretoken.go`, `vocab.go`, `cl100k_base.tiktoken`, `requesttokens.go` (Task 8 añade `requesttokens.go`: `CountTokens`/`CountMessageTokens`/`CountToolsTokens` — relocalizados desde `internal/streamingopenai/tokencount.go` (Task 7), que los tenía como funciones privadas del paquete — más `CountSystemTokens`, nuevo, para el tercer parámetro de `estimate_request_tokens` (`system_prompt`, `tokenizer.py:256-293`) que `streaming_anthropic.py` necesita y `streaming_openai.py` no usa. Ver ledger en `task-8-report.md`: ambos paquetes de formatter (`streamingopenai`, `streaminganthropic`) consumen estas funciones vía `internal/tokenizer`; sus dos call sites de streaming (`streaming_openai.py:317-320` fallback, `streaming_anthropic.py:177-183` message_start vía `estimate_request_tokens`) pasan `apply_claude_correction=False`. El parámetro `applyCorrection bool` SÍ se porta en `CountMessageTokens`/`CountToolsTokens`/`CountSystemTokens` (Task 8 fix round 1) porque el endpoint `/v1/messages/count_tokens` (`routes_anthropic.py:948-952`, Task 10) llama `estimate_request_tokens(apply_claude_correction=True)`. Igual que upstream, `applyCorrection` escala SOLO el total final por `CLAUDE_CORRECTION_FACTOR` (1.15) truncando hacia cero (`int()`), nunca los sub-conteos por-string (que siempre pasan `false`). No se porta `estimate_request_tokens` como función propia: sus tres llamadas (`count_message_tokens` + `count_tools_tokens` + `count_system_tokens`) se inlinean directamente en `streaminganthropic.Formatter.EmitMessageStart`, ver task-8 ledger.) |
| `parsers.py` | `internal/parsers` | `parser.go`, `toolcalls.go`, `brace_scanner.go`, `dedup.go`, `truncation.go` (Task 1 porta find_matching_brace, deduplicate_tool_calls, parse_bracket_tool_calls y el state machine AwsEventStreamParser.feed/get_tool_calls/reset — ports de `parsers.py:39-569`. `Event.Kind` usa los 3 tipos que `_process_event` realmente emite (`content`/`usage`/`context_usage`, .upstream/kiro/parsers.py:308-332), no los 7 nombres de prefijo: `tool_start`/`tool_input`/`tool_stop` nunca producen evento en el original — solo mutan estado — y `followupPrompt` como prefijo más temprano no tiene rama en absoluto (cae al `return None` final). `Finish()` y el Kind `tool_call` son adiciones Go-only sin equivalente upstream (el original expone tool calls solo vía `get_tool_calls()`, que también se porta 1:1 como `Parser.GetToolCalls()`). Los ids de `parse_bracket_tool_calls` dependen de un contador de sesión no reproducible fuera de la suite completa de grabación (docs/CORPUS.md §"contadores globales de sesión"); el test de corpus compara forma (`call_<8 hex>`), no valor, igual que ya hace el port para chatcmpl-/msg_-hex. Test diferencial `bytes.ToValidUTF8` vs `decode('utf-8', errors='ignore')` (spec §6.4/§8.4, pendiente desde fase 2) añadido en `differential_test.go`, 1000 semillas fijas, verificado contra python3 real.) |
| `thinking_parser.py` | `internal/thinkingparser` | `state_machine.go`, `parser_test.go` (Task 2 porta el FSM de tres estados (PreContent/InThinking/Streaming) que detecta 4 tags de razonamiento (`<thinking>`, `<think>`, `<reasoning>`, `<thought>`) al inicio del stream. Implementa buffering prudente con `max_tag_length = max(len(tag)) * 2` para evitar partir tags entre chunks. Feed/Finish devuelven `(thinking, content)` en lugar del `ThinkingParseResult` completo del original, y aplican el handling mode internamente en lugar de dejar `process_for_output` como método separado.) |
| `truncation_state.py` | `internal/truncationstate` | `cache.go`, `cache_test.go` (Task 3 porta los dos `dict` protegidos con `threading.Lock` como caches de `any` con protección `sync.Mutex`, índice de contenido con SHA256 hash del primer 500 caracteres, Get destructivo — `map.pop(key)` ↔ `delete(map, key)` — retorna `false` si no existe. Tests hand-written: SetTool + GetTool retorna y elimina, SetContent + GetContent con SHA256, concurrencia con dos fases secuenciales: 10 goroutines para set, 10 para get.) |
| `truncation_recovery.py` | `internal/truncationrecovery` | `recovery.go`, `recovery_test.go` (Task 4 porta `generate_truncation_tool_result(tool_name, tool_use_id, truncation_info) -> dict` y `generate_truncation_user_message() -> string` y `should_inject_recovery() -> bool` del original. Implementa un package var `truncationRecoveryEnabled` que espeja la bandera de converterscore; los tests pueden sobrescribir localmente. 17 fixtures de corpus (13 para tool_result, 2 para user_message, 2 para should_inject, con algunos casos usando kwargs en lugar de args, y TRUNCATION_RECOVERY tanto true como false en config). Tests manejan ambas formas de input (args vs kwargs) y leen/aplican la config dinámicamente.) |
| `payload_guards.py` | `internal/payloadguards` | |
| `cache.py` | `internal/cache` | |
| `model_resolver.py` | `internal/modelresolver` | |
| `kiro_errors.py` | `internal/kiroerrors` | `enhance.go` |
| `network_errors.py` | `internal/networkerrors` | `types.go`, `classify.go`, `messages.go` |
| `account_errors.py` | `internal/accounterrors` | `classify.go` |
| `exceptions.py` | `internal/validationerrors` | `validation.go` |
| `models_openai.py` | `internal/modelsopenai` | `models.go` |
| `models_anthropic.py` | `internal/modelsanthropic` | `blocks.go`, `models.go` |
| `converters_core.py` | `internal/converterscore` | `types.go`, `extract.go`, `text.go`, `tools.go`, `images.go`, `normalize.go`, `thinking.go`, `payload.go` |
| `converters_openai.py` | `internal/convertersopenai` | `converters.go` |
| `converters_anthropic.py` | `internal/convertersanthropic` | `converters.go` |
| `streaming_core.py` | `internal/streamingcore` | `events.go`, `pipeline.go`, `pipeline_test.go` (Task 6 porta la clase KiroEvent y el pipeline que une parsers.Parser (Task 1) y thinkingparser.Parser (Task 2) en un stream de eventos unificado. El pipeline procesa eventos del parser y aplica thinking splitting: para cada evento "content", extrae el valor, lo pasa al thinking parser, y emite eventos "thinking" (si hay) y "content" (si hay) en ese orden. Maneja también "usage" → UsageData, "context_usage" → float, "tool_call" → ToolUseData. Si hay diagnóstico de truncamiento, emite un evento "error" con el mensaje del diagnóstico. Feed() procesa un chunk, Finish() vacía buffers pendientes.) |
| `streaming_openai.py` | `internal/streamingopenai` | `formatter.go`, `tokencount.go`, `formatter_test.go` (Task 7, fix round 2. Formatter struct mantiene estado: completionID (chatcmpl-<32hex>), model, created (timestamp unix), firstChunk, acumuladores de content/thinking, tool calls, `creditsUsedRaw` (bytes JSON crudos del evento "usage", ecoados tal cual en `usage.credits_used` — ver `streaming_openai.py:405-406`, `final_chunk["usage"]["credits_used"] = metering_data`, que no es siempre un float), `contextUsagePercentage`, `requestMessages`/`requestTools` (JSON crudo, para el fallback de `count_message_tokens`/`count_tools_tokens` cuando Kiro no manda `context_usage_percentage` — streaming_openai.py:317-323; el corpus muestra que esto ocurre en 39/40 fixtures, no es un caso raro). `tokencount.go` (Task 8 update: ahora solo `isJSONTruthy`, dialect-specific — `count_tokens`/`count_message_tokens`/`count_tools_tokens` se movieron a `internal/tokenizer/requesttokens.go` como funciones exportadas, reutilizadas por `streaminganthropic`; `formatter.go` llama `tokenizer.CountTokens`/`CountMessageTokens`/`CountToolsTokens` en vez de las versiones locales que tenía hasta el fix round 2 — ver ledger en `task-8-report.md`) reimplementaba `count_tokens`/`count_message_tokens`/`count_tools_tokens` de `kiro/tokenizer.py` con paridad exacta, incluyendo el factor de corrección Claude (1.15, solo aplicado a completion_tokens) y `pyjson.Dumps` para los sub-JSON (tool `parameters`/`input_schema`, tool_use `input`) cuyo orden de claves afecta el conteo de tokens. Finish() calcula completion_tokens vía tokenizer+corrección, determina finish_reason (truncation vs tool_calls vs stop, replicando el orden de streaming_openai.py:296-302), calcula prompt/total_tokens vía `calculate_tokens_from_context_usage` (streaming_core.py:337-362, incluyendo la división `/100` que el fix round 1 omitía) con fallback a `count_message_tokens`+`count_tools_tokens`. 39/40 fixtures del corpus pasan; el fixture restante (`c272a07c05e0e8a5`) depende de un mock de `parse_bracket_tool_calls` en el test Python de origen que no es reproducible desde los datos del fixture — ver `task-7-report.md`. Task 8 convierte ese fallo en un `t.Skip` explícito con cita de línea, en vez de dejarlo como fallo silencioso sin marcar (mismo caso que 4 fixtures de `streaminganthropic`, ver abajo). Configurable thinking handling mode: AsReasoningContent vs AsContent.) |
| `streaming_anthropic.py` | `internal/streaminganthropic` | `formatter.go`, `block_index.go`, `finish.go`, `payloads.go`, `formatter_test.go` (Task 8. `block_index.go`: `blockIndex` — índice de bloque corriente + estado open/index para thinking y text; tool_use se abre y cierra átomicamente dentro de una sola llamada (`ReserveToolBlock`), sin estado persistente. `Formatter`: `messageID` (`msg_<24hex>`, `utils.GenerateMessageID`), `model`, `thinkingHandling` (as_reasoning_content/include_as_text/strip), `thinkingSignature` (`sig_<32hex>`, generado una vez en `New`, reusado en cada bloque thinking — igual que el original, que genera un único `thinking_signature` antes del loop), acumuladores fullContent/fullThinkingContent, toolBlockCount, receivedContextUsage, cache_read/creation_input_tokens (punteros, omitempty — solo aparecen en el `usage` de `message_delta` si un evento "usage" los trajo), requestMessages/requestTools/requestSystem (JSON crudo). `EmitMessageStart` calcula `input_tokens` inline con `tokenizer.CountMessageTokens`+`CountToolsTokens`+`CountSystemTokens` (streaming_anthropic.py:176-183, `estimate_request_tokens(apply_claude_correction=False)` — no se portó como función separada, sus tres llamadas se inlinearon). `Handle` despacha por `Kind`: "content" cierra thinking si está abierto y abre/reusa el bloque text; "thinking" se ramifica por `thinkingHandling` (bloque thinking nativo / fold a texto / strip silencioso, pero `fullThinkingContent` acumula SIEMPRE, incluso en strip — afecta output_tokens); "tool_use" cierra thinking y text, emite el triplete start/delta/stop; "context_usage" solo marca `receivedContextUsage=true` (el pipeline ya filtra `is not None`); "usage" fusiona campos de caché vía `_extract_cache_usage_fields` (streaming_anthropic.py:101-126). `Finish`: detecta tool calls estilo `[Called f with args: {...}]` post-loop vía `internal/parsers.ParseBracketToolCalls` (ya portado en Task 1 — NO se reimplementó), cierra bloques que sigan abiertos, calcula `content_was_truncated` (sin context_usage + hay texto + cero tool blocks), `output_tokens` vía `tokenizer.CountTokens(fullContent+fullThinkingContent, true)` (con corrección Claude — este call site SÍ usa el default `apply_claude_correction=True`, a diferencia de `estimate_request_tokens`), `stop_reason` (max_tokens > tool_use > end_turn), y emite `message_delta`+`message_stop`. **Efecto secundario DIFERIDO — persistencia de truncation-state**: upstream `stream_kiro_to_anthropic`, tras `message_stop` (streaming_anthropic.py:665-686), si `should_inject_recovery()` (Task 4, devuelve `TRUNCATION_RECOVERY`, **default true** — config.py:333) persiste la info de truncación en los caches de `internal/truncationstate` (Task 3) vía `save_tool_truncation`/`save_content_truncation`, recolectando además `truncated_tools` durante el loop de tool_use (chequeo `_truncation_detected`, streaming_anthropic.py:470-475) — para que la SIGUIENTE petición inyecte los mensajes de recovery. Este port NO ejecuta ese save en `streaminganthropic.Finish`: no tiene salida observable en SSE (por eso ningún fixture del corpus lo ejercita y el port pasa igual) y requiere los paquetes `truncationstate`+`truncationrecovery`, que el formatter no recibe. **Pendiente para Task 10/routesanthropic** (que orquesta request/response y ya leerá truncation-state para inyectar recovery): tras cerrar el stream debe recolectar los tool calls truncados (vía el Kind `"error"`/diagnosis que `Handle` hoy ignora a propósito, formatter.go:125-129, o `parsers.Parser.TruncationDiagnosis`) y el `full_content`, y llamar el save gated por `truncationrecovery.ShouldInjectRecovery(converterscore.TruncationRecoveryEnabled)`. Al ser default-on, omitir este cableado en Task 10 rompería el recovery silenciosamente (misma clase de riesgo que el Critical de Task 4). `EmitError` — sin equivalente-método en el original, que hace `yield format_sse_event("error", ...)` inline en el `except Exception` del generador — expone ese mismo evento para que el caller (fuera de este paquete: la capa HTTP que consume el stream de Kiro) lo dispare cuando la lectura falla a mitad de stream; `FirstTokenTimeoutError` NO dispara este evento (streaming_anthropic.py:695-696, re-raise silencioso). No se porta la intercepción MCP de `web_search` (streaming_anthropic.py:354-468, Path B): requiere una llamada de red real a `kiro.mcp_tools.call_kiro_mcp_api`, pertenece a una capa de ejecución de herramientas fuera del alcance de un formatter SSE, y ningún fixture de `stream_kiro_to_anthropic` la ejercita (el único fixture con una tool `web_search` en `request_tools`, `d3fa86d2d3539b88`, solo emite un evento "content", nunca un "tool_use" nombrado "web_search" — se prueba la estimación de tokens de la definición de la tool, no la intercepción). Corpus: `format_sse_event/` 83/83 (`formatter_test.go`, opción (a) del brief: se testea `internal/sse.FormatEvent` + `internal/pyjson.Dumps` directamente contra estos fixtures, sin pasar por `Formatter`, porque no dependen de ningún estado generado — comparación byte a byte, no hay message-id ni nada no determinista en esta capa). `stream_kiro_to_anthropic/` 16/20 pass + 4/20 skip documentado (0 fail) — ver ledger abajo. **Ruling — comparación por forma (`message_id`)**: igual que Task 7's `chatcmpl-<32hex>`. El corpus se grabó con `uuid.uuid4()` monkeypatcheado a un contador global incremental por sesión de test (`internal/utils/ids.go`'s "Contrato de congelación"), así que cada fixture trae el valor que tocaba en SU posición del contador global — no reproducible sin recrear el orden exacto de ejecución de toda la suite Python. `formatter_test.go` verifica el id por regex `^msg_[0-9a-f]{24}$` en ambos lados (actual y esperado) y lo normaliza antes de comparar el resto del payload por igualdad estructural. **Ruling — cross-package tokencount**: movidas a `internal/tokenizer/requesttokens.go` (exportadas: `CountTokens`, `CountMessageTokens`, `CountToolsTokens`, `CountSystemTokens` nueva), no aliased ni reimplementadas. Se descartó (a) exportar directamente desde `internal/streamingopenai` porque un formatter de un dialecto dependiendo del paquete interno de otro dialecto es una capa más rara que un paquete `tokenizer` compartido, y el brief lista `internal/tokenizer` como paquete consumido por Task 8; (b) reimplementar desde cero en `streaminganthropic` porque duplicaría ~250 líneas ya probadas contra corpus por Task 7. `internal/streamingopenai/tokencount.go` quedó con solo `isJSONTruthy` (dialect-specific, gate de "usage event recibido"); `formatter.go` de ese paquete se actualizó para llamar `tokenizer.CountTokens`/`CountMessageTokens`/`CountToolsTokens` — comportamiento idéntico, confirmado corriendo su test suite antes/después del refactor (mismo resultado: 39/40 + el fixture ahora explícitamente skipeado). **Ruling — 4 fixtures skipeados con prueba** (misma clase que el caso de monkey-patch de Task 7): `b0f2a0cabe68929e`, `e21af20711a4832e`, `f0f3c8b21b133a12` tienen `estimate_request_tokens` mockeado con `return_value={"total_tokens": N}` fijo en el test Python de origen (`test_streaming_anthropic.py:1105,1171,1139` respectivamente — confirmado por grep), desconectado del contenido real de `request_messages`/`request_tools`/`request_system`; la implementación real de este port (`tokenizer.CountMessageTokens`+`CountToolsTokens`+`CountSystemTokens`) computa 11/9/26 respectivamente para esos mismos inputs — valores distintos por construcción, no por bug. `51e4de551161d0ff` tiene `parse_bracket_tool_calls` mockeado a un resultado no vacío inventado por el test (`test_streaming_anthropic.py:463`) para contenido `"[tool_call: func1]"`, que NO matchea el patrón real `\[Called\s+(\w+)\s+with\s+args:\s*` (`internal/parsers.ParseBracketToolCalls`, ya portado 1:1 en Task 1); TODOS los demás tests de `test_streaming_anthropic.py` mockean esa misma función a `return_value=[]`, confirmando que la función real nunca se ejercita a través de este corpus. Los 4 casos están marcados con `t.Skip(...)` con cita de línea en `formatter_test.go`, no como fallos silenciosos.) |
| `routes_openai.py` | `internal/routesopenai` | `handler.go`, `failover.go`, `stream.go`, `collect.go`, `handler_test.go` (Task 9 porta `GET /v1/models` y `POST /v1/chat/completions` en la rama account-system de `routes_openai.py:122-556` — la rama "legacy" de una sola cuenta sin failover, `routes_openai.py:558-769`, no se porta: este servicio siempre corre con `accountmanager.Manager`. `handler.go`: `Handler`/`New`, `Models`, `thinkingHandling` (mapea `FAKE_REASONING_HANDLING` a `streamingopenai.ThinkingHandling`), el sobre de error unificado `writeOpenAIError`. `failover.go`: `ChatCompletions` (decodifica el body), `failoverChatCompletions` (bucle `exclude`+`GetNextAccount` sin contador `MAX_ATTEMPTS` explícito — confía en que `GetNextAccount` ya agota vía cuarentena+exclude), `attemptAccount`, `handleKiroError`/`handleTransportError` (clasificación Fatal/Recoverable), `writeExhausted`. `stream.go`/`collect.go`: el bucle explícito D4 (`drivePipeline`, sin goroutines) que alimenta `streamingcore.Pipeline`→`streamingopenai.Formatter`; el modo no-streaming (`collectResponse`, port de `collect_stream_response`, streaming_openai.py:576-688) NO lee los acumuladores internos del Formatter — re-parsea los mismos bytes SSE que produciría el modo streaming, igual que hace el original. **Cuatro desviaciones documentadas frente al brief/upstream** (razonadas en el comentario de cabecera de `handler.go`): (1) `owned_by` en `/v1/models` es `"anthropic"` (routes_openai.py:151), no `"kiro"` como decía el brief — upstream gana; (2) dialecto de error unificado `{"error":{"message","type":"kiro_api_error","code"}}` para TODAS las respuestas de error de esta ruta (incluida cuenta-agotada), no solo para las JSONResponse explícitas del original — sigue el spec §7.1, más consistente para un cliente OpenAI; (3) Kiro se llama SIEMPRE con `stream=true` en `RequestWithRetry` (routes_openai.py:358-363/612-617), independientemente de `request_data.stream`, que solo decide el formato de la respuesta al cliente; (4) fallos de transporte (`*httpclient.RequestError`) se tratan siempre como Recoverable en el failover, sin pasar por `accounterrors.Classify` (que trataría cualquier `SuggestedHTTPCode` 5xx como Fatal) — replica el override explícito de `ErrorType.RECOVERABLE` de routes_openai.py:512-517 para este caso, ya que `accountmanager.Manager.ReportFailure` no admite forzar la clasificación. **Seam de test**: `Handler.apiURL` (campo no exportado, `handler.go`) permite a `handler_test.go` apuntar al `httptest.Server` falso — `auth.Manager.APIHost()` siempre devuelve un host real bajo `*.kiro.dev` sin override público, mismo patrón que `accountmanager.Manager.listURLOverride`. 6/6 escenarios del brief cubiertos en `handler_test.go`, con dos ajustes deliberados: el escenario de failover usa 402 en vez de 500 (un 500 real de Kiro es Fatal según la tabla de `accounterrors`, no dispararía failover) y el escenario de cuenta agotada usa dos cuentas con 402 constante en vez de un `500`x3 literal, por la misma razón.) |
| `routes_anthropic.py` | `internal/routesanthropic` | |
| `http_client.py` | `internal/httpclient` | `transport.go`, `proxy.go`, `client.go`, `stream.go` |
| `auth.py` | `internal/auth` | `types.go`, `manager.go`, `json_source.go`, `oidc.go`, `sqlite.go`, `refresh.go` (Task 1-2 porta `types.go`, `manager.go` (constructores + `AccessToken`/`ProfileARN`, stub de `ForceRefresh`), `json_source.go`, stubs de `oidc.go`/`sqlite.go`. Task 3 completa `oidc.go` con `refreshAWSSSO`/`doAWSSSORefreshAttempt` (ports de `auth.py:743-869`) y `loadEnterpriseDeviceRegistration` (port de `auth.py:458-487`). Task 4 completa `sqlite.go` (`loadFromSQLite`/`saveToSQLite` — ports de `auth.py:248-382` y `auth.py:388-633`). Task 5 implanta `refresh.go` con `Refresh()` singleflight (port de `auth.py:870-934` `get_access_token`), graceful degradation (SQLite+400, `auth.py:906-919`), y reestructura `AccessToken`/`ForceRefresh`) |
| `account_manager.py` | `internal/accountmanager` | `types.go`, `discovery.go`, `state.go`, `manager.go`, `manager_test.go`, `breaker.go`, `selection.go`, `failover.go`, `failover_test.go`, `init.go`, `init_test.go`, `integration_test.go` (Task 6 porta `types.go` (Account, AccountStats, ModelAccountList), `discovery.go` (loadCredentials, processRefreshTokenEntry, processFileEntry, scanDirectory, processFile, isValidJSONFile, isValidSQLiteFile — ports de `account_manager.py:127-326`), `state.go` (LoadState, SaveState, renameWithRetry — ports de `account_manager.py:328-416`), `manager.go` (Manager struct, NewManager, LoadCredentials, SaveStatePeriodically, hasStateChanged, Accounts — ports de `account_manager.py:179-214` y las rutas del state loop). Task 7 implanta circuit breaker (breaker.go — quarantineWindow, isInQuarantine), sticky selection (selection.go — nextEnabledIdx), failover (failover.go — GetNextAccount, ReportSuccess, ReportFailure — todos ports de `account_manager.py:645-867`). Task 8 implanta model catalog (init.go — Initialize, refreshAccountModels, GetAllAvailableModels, GetFirstAccount, fallbackModels — ports de `account_manager.py:432-644, 868-897` y config.py:276-290). Task 9 implanta integration test (`integration_test.go` — TestPhase4Integration que verifica wiring de Manager + auth.Manager + httpclient.Client contra fake Kiro server: 200 success, 429 retry, 500 failover).) |
| `mcp_tools.py` | `internal/mcptools` | |
| `debug_logger.py` | `internal/debuglogger` | |
| `debug_middleware.py` | `internal/debugmiddleware` | |
| `__init__.py` | *(sin equivalente: solo reexporta)* | |

## Duplicación temporal: `model_resolver.py`

`internal/convertersanthropic/converters.go` (Task 9) porta, como funciones NO
exportadas (`normalizeModelName`, `getModelIDForKiro`) más el package var
`HiddenModels`, el subconjunto mínimo de `model_resolver.py` que
`anthropic_to_kiro` necesita para calcular el `modelId` que manda a Kiro
(`normalize_model_name` + `get_model_id_for_kiro`, sin la clase
`ModelResolver` completa, sin caché dinámica ni alias — el propio original
describe `get_model_id_for_kiro` como "a simple helper for converters that
don't have access to the full ModelResolver"). Se hizo así porque
`internal/modelresolver` (fila de arriba) todavía no existe y Task 9 no puede
crear paquetes fuera de `internal/convertersanthropic/*`. Cuando una tarea
futura implemente `internal/modelresolver`, debería sustituir ese subconjunto
en `converters.go` por una llamada real a ese paquete en vez de mantener dos
copias del mismo algoritmo.

`internal/convertersopenai/converters.go` (Task 10) repite exactamente la
misma duplicación, por la misma razón y con la misma restricción de alcance
(Task 10 solo puede tocar `internal/convertersopenai/*` y este fichero):
`build_kiro_payload` del original también llama a
`get_model_id_for_kiro(request_data.model, HIDDEN_MODELS)`. En vez de
importar `convertersanthropic` (sus dos funciones no están exportadas, y
acoplar un adaptador a los internals de otro no reduce el radio de impacto),
este paquete lleva su propia copia de `normalizeModelName` +
`getModelIDForKiro` + `HiddenModels`. Cuando `internal/modelresolver` exista,
la tarea que lo cree debería sustituir las TRES copias (converterscore no
tiene una propia — solo los dos adaptadores) por una llamada real a ese
paquete.

## Paquetes que no existen en el original

| Paquete Go | Responsabilidad | Por qué existe |
|---|---|---|
| `internal/sse` | Formateo de eventos SSE (Task 5) | Rompe el ciclo `mcp_tools` ↔ `streaming_anthropic` (FormatEvent con dialecto OpenAI/Anthropic, FormatDone OpenAI-only) |
| `internal/pyjson` | Emula `json.dumps` y `str()` de Python | Aísla las rarezas de Python en un solo sitio |
| `internal/testutil` | Cargador del corpus y generadores de chunks | Equivalente de los helpers de `conftest.py` |
| `internal/server` | Router, middleware y ciclo de vida | Equivalente del `lifespan` de FastAPI |
| `internal/version` | Cadena de versión | En Python es una constante en `config.py` |

## Renombrados

| Python | Go | Motivo |
|---|---|---|
| `exceptions.py` | `internal/validationerrors` | «exceptions» no significa nada en Go |

## Ciclos de importación rotos

| Ciclo en Python | Cómo se rompe |
|---|---|
| `utils` ↔ `auth` | `utils` declara la interfaz `TokenProvider` y `auth.Manager` la satisface |
| `mcp_tools` ↔ `streaming_anthropic` | El formateo de SSE se extrae a `internal/sse` |

## Extensiones deliberadas respecto al original

| Dónde | Qué | Por qué |
|---|---|---|
| `internal/auth.AuthType` | 5 valores (`Unknown`, `KiroDesktop`, `AWSSSO`, `KiroCLI`, `RefreshOnly`) en vez de los 2 del `Enum` original (`KIRO_DESKTOP`, `AWS_SSO_OIDC`, `auth.py:68-83`) | Reflejan los tres `type` que ya distingue `credentials.json` (`json`, `sqlite`, `refresh_token`, spec §6.11) en vez de que kiro-cli y refresh-token-only colapsen en `KIRO_DESKTOP` como hoy hace el Python |
| `internal/auth` (JSON source) | La detección de región por ARN (`auth.py:357-366`, solo en el loader de SQLite en el original) se generaliza como fallback también para el JSON de Kiro Desktop | Pedido explícitamente por el plan de la Task 2 (4 niveles de precedencia de región, incluido "detectado del ARN") y reutilizable sin cambios por la Task 4 (SQLite) |
